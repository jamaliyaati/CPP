#include <iostream>10
#include <cmath>
using namespace std;

int main (){

    double x;
    double y;
    double z;

    cout << "num 1 ";
    cin >> x;
    //cout << "num 2 ";
    //cin >> y;

   // z = max(x,y);
   // z = min(x,y);
   // z = pow(x, y);           // X TO THE POWER OF Y
   // z = sqrt(x);             // SQUARE ROOT OF X
   // z = abs(x);              //  POSITIVE NUMBER CONVERTED TO NEGATIVE
   // z = round(x);            // NORMAL ROUNDING OF THE VALUE
   // z = ceil(x);             // ALWAYS ROUNDS UP THE VALUE
   // z = floor(x);            // ALWAYS ROUNDS UP THE VALUE



    cout << "ans = " << z << endl;



        return 0;
}